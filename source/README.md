# Content Source

This directory follows the Hexo authoring convention.

- Put published posts in `source/_posts/**`.
- Keep Hexo-style frontmatter such as `title`, `date`, `abbrlink`, `tags`, `categories`, `toc`, and `math`.
- Astro reads this directory through `src/content.config.ts` and compiles the static site.
